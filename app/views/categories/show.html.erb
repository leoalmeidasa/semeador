<div class="max-w-xl mx-auto bg-white p-6 mt-10 rounded-xl shadow-md border border-gray-200">
  <h2 class="text-2xl font-bold text-green-700 mb-4">Detalhes da Categoria</h2>

  <p><strong>Nome:</strong> <%= @category.name %></p>
  <p><strong>Total de transações:</strong> <%= @category.transactions.count %></p>

  <div class="mt-6 space-x-3">
    <%= link_to "Editar", edit_category_path(@category), class: "text-blue-600 hover:underline" %>
    <%= link_to "Voltar", categories_path, class: "text-gray-600 hover:underline" %>
  </div>
</div>
