<div class="max-w-xl mx-auto bg-white p-6 mt-10 rounded-xl shadow-md border border-gray-200">
  <h2 class="text-2xl font-bold text-green-700 mb-4">Detalhes da Meta</h2>

  <p><strong>Título:</strong> <%= @goal.title %></p>
  <p><strong>Valor Alvo:</strong> R$ <%= number_to_currency(@goal.target_amount, unit: "") %></p>
  <p><strong>Progresso Atual:</strong> R$ <%= number_to_currency(@goal.progress, unit: "") %></p>
  <p><strong>Percentual:</strong> <%= ((@goal.progress.to_f / @goal.target_amount) * 100).round(1) %>%</p>
  <p><strong>Data Limite:</strong> <%= l(@goal.due_date) %></p>

  <div class="mt-6 space-x-3">
    <%= link_to "Editar", edit_goal_path(@goal), class: "text-blue-600 hover:underline" %>
    <%= link_to "Voltar", goals_path, class: "text-gray-600 hover:underline" %>
  </div>
</div>
